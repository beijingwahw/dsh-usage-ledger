diff --git a/lib/index.js b/lib/index.js
index 357a18bac52de13f1f4b05e454faeb59d55e4487..f585b40eeeef6de5ade77eb1249c849d904a2335 100644
--- a/lib/index.js
+++ b/lib/index.js
@@ -170,10 +170,17 @@ var Hmr = class extends Service {
 	async _resolve(specifier, parentURL, attrs) {
 		switch (this.internal.version) {
 			case "v1": return await this.internal.resolve(specifier, parentURL, attrs);
-			case "v2": return this.internal.resolveSync(parentURL, {
-				specifier,
-				attributes: attrs
-			});
+			case "v2": {
+				try {
+					return this.internal.resolveSync(parentURL, {
+						specifier,
+						attributes: attrs
+					});
+				} catch (err) {
+					if (err?.code !== "ERR_INVALID_ARG_TYPE") throw err;
+					return this.internal.resolveSync(specifier, parentURL);
+				}
+			}
 		}
 	}
 	async *[Service.init]() {
